/** The single canonical host used by metadata, robots, sitemaps, and schema. */
export const SITE_URL = "https://www.royal-smash.cc";

/** Stable content date; do not pretend every URL changed whenever sitemap.xml is requested. */
export const CONTENT_UPDATED_AT = new Date("2026-09-13T00:00:00.000Z");
